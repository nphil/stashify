# Stashify Windows runner deps (installed into the runner venv by install.ps1).
# torch + torchvision are installed separately from the CUDA wheel index.
requests
psutil
av
spandrel
PySide6-Essentials     # tray flyout GUI
onnxruntime-directml   # live-preview mosaic scan (rfdetr onnx via DirectX; no CUDA/cuDNN)
